<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        Reading the user's photos and videos is unavoidable for a backup app: it has to find new
        media on its own, which rules out the Photo Picker (a manual choice every time) and
        Selected Photos Access (cannot discover anything new). This is the same access Samsung's
        own gallery sync uses. The app only ever READS this media — it never deletes or modifies it.

        READ_MEDIA_VISUAL_USER_SELECTED is declared deliberately. On Android 14+ the user can grant
        access to only some photos, and a backup app must notice that and say so, rather than
        quietly backing up a fraction of someone's library and reporting success.
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- Android 12 and below predate the granular media permissions above. -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!--
        usesCleartextTraffic is already false by default at targetSdk 28+, but stating it is worth
        the line: it survives a future targetSdk change, and it makes the intent explicit for
        anyone reading the manifest. Every endpoint this app touches is HTTPS, and a plain-http
        URL slipping in should fail loudly rather than quietly send someone's photos in the clear.
    -->
    <application
        android:name=".GallerySyncApplication"
        android:allowBackup="true"
        android:usesCleartextTraffic="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GallerySync">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.GallerySync"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            WorkManager initialises itself through App Startup by default, which happens before
            Hilt is ready and leaves it holding the default worker factory. BackupWorker takes
            constructor-injected dependencies, so it would fail to construct at the moment it runs.
            Removing this initializer hands configuration to GallerySyncApplication instead.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <provider
            android:name=".provider.MediaContentProvider"
            android:authorities="com.gallery.sync.provider"
            android:exported="false"
            android:grantUriPermissions="true" />

        <!--
            MSAL's redirect target. The sign-in browser tab hands the authorization code back
            here, and MSAL takes it from there.

            The path below is the RAW signature hash, while msal_config.json carries the
            URL-encoded form of the same value ("+" as %2B, "=" as %3D). That asymmetry is
            required — encoding this one, or decoding that one, breaks the redirect.
        -->
        <activity
            android:name="com.microsoft.identity.client.BrowserTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="msauth"
                    android:host="com.gallery.sync"
                    android:path="/E7ZHhkRNGcbq5T+gUNCTkroYYMo=" />
            </intent-filter>
        </activity>
    </application>

</manifest>
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".GallerySyncApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GallerySync">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.GallerySync"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".provider.MediaContentProvider"
            android:authorities="com.gallery.sync.provider"
            android:exported="false"
            android:grantUriPermissions="true" />

        <!--
            MSAL's redirect target. The sign-in browser tab hands the authorization code back
            here, and MSAL takes it from there.

            The path below is the RAW signature hash, while msal_config.json carries the
            URL-encoded form of the same value ("+" as %2B, "=" as %3D). That asymmetry is
            required — encoding this one, or decoding that one, breaks the redirect.
        -->
        <activity
            android:name="com.microsoft.identity.client.BrowserTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="msauth"
                    android:host="com.gallery.sync"
                    android:path="/E7ZHhkRNGcbq5T+gUNCTkroYYMo=" />
            </intent-filter>
        </activity>
    </application>

</manifest>
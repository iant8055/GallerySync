# Android build
*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.ap_
*.dex

# Keys and secrets
*.jks
*.keystore
*.p12
google-services.json
local.secrets.properties
# Google Cloud Console's own download filename for an OAuth client's credentials — the Web-type
# client's copy carries a real client_secret. Ian, 22 Sept 2026: dropped one of these straight into
# the repo root while setting up the Google Photos OAuth client; caught before it was ever staged,
# but never again on trust alone.
client_secret_*.json

# Test coverage
*.exec
reports/

# OS
Thumbs.db
desktop.ini

# Node (if companion tools added later)
node_modules/
design/gallerysync-app-directions.html
